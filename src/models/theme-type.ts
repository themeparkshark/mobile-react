export interface ThemeType {
  readonly back_button_url: string;
  readonly base_button_url: string;
  readonly bottom_bar_url: string;
  readonly coin_animation_url: string;
  readonly explore_button_url: string;
  readonly id: number;
  readonly leaderboard_button_url: string;
  readonly news_button_url: string;
  readonly primary_color: string;
  readonly profile_button_url: string;
  readonly secondary_color: string;
  readonly task_list_button_url: string;
  readonly verified_badge_url: string;
}
