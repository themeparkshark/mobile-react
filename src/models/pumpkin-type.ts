export interface PumpkinType {
  readonly id: number;
  readonly latitude: string;
  readonly longitude: string;
  readonly active_from: string;
  readonly active_to: string;
  readonly pumpkins: number;
}
